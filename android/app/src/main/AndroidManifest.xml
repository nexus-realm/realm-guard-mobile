<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <!-- Scan des QR codes de configuration TOTP. La caméra n'est pas requise
         pour faire fonctionner l'app (ajout manuel possible). -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false"/>
    <application
        android:label="Realm Guard"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Remplissage automatique : activité Flutter dédiée (entrypoint
             autofillEntryPoint), lancée par le service d'autofill de l'OS.
             FLAG_SECURE est appliqué côté natif (présente des identifiants). -->
        <activity
            android:name=".AutofillActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
        </activity>

        <!-- Pointeur du plugin flutter_autofill_service vers l'activité ci-dessus. -->
        <meta-data
            android:name="com.keevault.flutter_autofill_service.ACTIVITY_NAME"
            android:value="fr.nexusrealm.realmguard.AutofillActivity" />

        <!-- Le service d'autofill est déclaré par le plugin (fusion de manifeste) ;
             on personnalise son libellé et le texte de déverrouillage. -->
        <service
            android:name="com.keevault.flutter_autofill_service.FlutterAutofillService"
            android:label="Realm Guard"
            tools:replace="android:label">
            <meta-data
                android:name="com.keevault.flutter_autofill_service.unlock_label"
                android:value="Déverrouiller Realm Guard" />
            <meta-data
                android:name="com.keevault.flutter_autofill_service.unlock_drawable_name"
                android:value="ic_autofill_shield" />
        </service>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
